[gd_scene load_steps=2 format=3 uid="uid://drs27m0dqljl"]

[sub_resource type="GDScript" id="GDScript_y32ns"]
script/source = "extends CharacterBody2D

# Tốc độ di chuyển
const SPEED = 300.0
const JUMP_VELOCITY = -600.0

# Lấy trọng lực từ project settings
var gravity = ProjectSettings.get_setting(\"physics/2d/default_gravity\")

func _physics_process(delta):
	# Áp dụng trọng lực
	if not is_on_floor():
		velocity.y += gravity * delta
	
	# Xử lý nhảy
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY
	
	# Xử lý di chuyển trái phải
	var direction = Input.get_axis(\"ui_left\", \"ui_right\")
	if direction:
		velocity.x = direction * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
	
	move_and_slide()
	
	# Lật hình nhân vật theo hướng di chuyển
	if direction != 0:
		$Sprite2D.flip_h = direction < 0
"

[node name="Node2D" type="Node2D"]

[node name="CharacterBody2D" type="CharacterBody2D" parent="."]
script = SubResource("GDScript_y32ns")

[node name="Sprite2D" type="Sprite2D" parent="CharacterBody2D"]
region_enabled = true
region_rect = Rect2(0, 0, 10, 10)

[node name="CollisionShape2D" type="CollisionShape2D" parent="CharacterBody2D"]

[node name="Camera2D" type="Camera2D" parent="."]
